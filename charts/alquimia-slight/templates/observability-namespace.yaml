{{- if and .Values.observability.enabled .Values.observability.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "alquimia-slight.observability.namespace" . }}
  labels:
    {{- include "alquimia-slight.labels" . | nindent 4 }}
    app.kubernetes.io/component: observability
{{- end }}
